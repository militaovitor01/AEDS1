#include <stdio.h>
#include <stdlib.h>

int main (){
   
    int raio = 1, area;

    area = 3.14 * (raio*raio);
    printf ("Um circulo de raio 1, possui area de %d", area);

    return 0;
}